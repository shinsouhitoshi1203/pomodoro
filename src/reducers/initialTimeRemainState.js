const initialTimeRemainState = {
	isAllowed: false,
	displayText: "",
	currentSession: ""
};

export default initialTimeRemainState;

/* 
    dispatch({
        type: "setIsAllowed", // variable
        payload: true
    });

    dispatch({
        type: "setCurrentSession",
        payload: session
    })

    dispatch({
        type: "setDisplayText",
        payload: [3,"mm"] // variable
    })
*/
